# Sprint 2 工作内容

目前后端没有给接口，所以先完成前端的基本布局，数据等待后续推进

## dashboard 界面

分为两类，一个是所有用户的api list列表，一个是自己的api list列表

### 所有用户的api list

去掉最后一列的operation list

显示创建者是谁

### 自己的api lsit

保留operation list

### 搜索

允许用户通过api 名字、url、创建者、status等信息搜索对应的数据

## api详情

用户应当可以点击dashboard中的api名字跳转到对应的api详情页面

页面应当使用card的组件来显示api的各种信息

应当在合适的位置显示edit mapping withdraw的按钮

最下方应该显示api的历史记录，来给用户显示该api是否经过更新

## schema mapping 功能的集成

将client给出的schema mapping功能集成到当前我的平台
